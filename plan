TODO:
    -- При добавлении задачи не сбрасывается дата в календаре
    -- Миксин для градиентов (с переменной)
    ; -- Поебаться с переносом перевода в отдельный файл модуля или JSON
    ;     (модуль только на топ уровне ошибка)
    -- подогнать размеры сообщений таймеров
    ; -- сообщение о выполнении всех задач (иногда глючило)
    -- RU не выделяется при ховере

Табы:
;    -- ресайз по загрузке как текст ареа ?
;    -- при добавлении нового списка переключаться на него
; 	(делать активным)

Другое:
    ; -- Нет стилей для надписей и не все проработаны
    -- Выделение текста при редактировании
;  -- При редактировании текста задачи не сразу срабатывает ресайз
;   -- Прописать когда кнопки активны, когда нет

Баги:
   --  Контур контекстного меню толстый
   --  Контекстное меню не скрывается, не срабатывает
   --  Кнопки мелкие
   --  Календарь сделать на всю ширину контентого блока
   --  Посмотреть выравнивание стикера важно и кнопки добавления вкладки
;    --  Перевод через подключение модуля, не сервера JSON
   --  Выравнивание флекса ли задачи (инпут и кнопки)
   --  Нотифы времени из-за спрятанных значков съехали, надо добавить отступ